This was the documentation for version up to and including v2.2.
We keep this here for now in case it is still useful to anyone, but the directory
`docs` is where we host the current Sphinx-based documentation.
